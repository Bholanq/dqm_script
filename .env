# ---------------------------------------------------------------------------
# Database connection  (credentials from environment variables)
# ---------------------------------------------------------------------------
# Required env vars:
  REDSHIFT_HOST      = my-cluster.abc123.us-east-1.redshift.amazonaws.com
  REDSHIFT_PORT      = 5239
  REDSHIFT_DATABASE  = edp_dev
  REDSHIFT_USER      = dqm_user
  REDSHIFT_PASSWORD  = s3cr3t

# SSL is enforced (sslmode=require) — Redshift clusters require it by default.