# ---------------------------------------------------------------------------
# Database connection  (credentials from environment variables)
# ---------------------------------------------------------------------------
# Required env vars:
  REDSHIFT_HOST      = alumis-analytics-sandbox-cluster.ckismoy96eq7.us-east-1.redshift.amazonaws.com
  REDSHIFT_PORT      = 5439
  REDSHIFT_DATABASE  = dev
  REDSHIFT_USER      = akshay.pathade@procdna.com
  REDSHIFT_PASSWORD  = Akshay@123

# SSL is enforced (sslmode=require) — Redshift clusters require it by default.